<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cqjtu.cms.mapper.CurriculumStudyMapper">

    <select id="getSumCredit" resultType="java.lang.String">
        SELECT SUM(credit) FROM curriculum_study WHERE sno=#{param1} AND result='通过' AND type=#{param2} AND attribute=#{param3}
    </select>
</mapper>
